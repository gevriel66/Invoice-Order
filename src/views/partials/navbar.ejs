<nav class="navbar navbar-expand navbar-custom sticky-top">
    <div class="container-fluid">
        <span class="navbar-brand text-muted fs-6">
            <i class="bi bi-building me-1"></i> Sistem Manajemen Dokumen Lokal
        </span>
        <div class="ms-auto d-flex align-items-center">
            <div class="me-3 text-end d-none d-sm-block">
                <div class="fw-semibold text-dark"><%= (typeof currentUser !== 'undefined' && currentUser) ? currentUser.name : 'Administrator' %></div>
                <div class="text-muted small"><%= (typeof currentUser !== 'undefined' && currentUser) ? currentUser.role : 'ADMIN' %></div>
            </div>
            <div class="dropdown">
                <a href="#" class="d-flex align-items-center text-dark text-decoration-none dropdown-toggle" data-bs-toggle="dropdown">
                    <div class="bg-primary text-white rounded-circle d-flex align-items-center justify-content-center me-2" style="width: 38px; height: 38px; font-weight: 600;">
                        <%= (typeof currentUser !== 'undefined' && currentUser && currentUser.username) ? currentUser.username.charAt(0).toUpperCase() : 'A' %>
                    </div>
                </a>
                <ul class="dropdown-menu dropdown-menu-end shadow border-0">
                    <li><a class="dropdown-item text-danger" href="/logout"><i class="bi bi-box-arrow-right me-2"></i> Logout</a></li>
                </ul>
            </div>
        </div>
    </div>
</nav>
